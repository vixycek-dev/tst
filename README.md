# Website Only

A minimal static website scaffold.

Open `index.html` in your browser to view locally, or serve with a static server.

Quick local server (Python 3):

```bash
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
